/**
 * resolve a package version by resolving package.json
 *
 * @param pkgName dependency to loockup
 */
export declare function resolvePackageVersion(pkgName: string): string;
export declare function parseEventId(eventId: string): {
    blockHeight: number;
    eventId: number;
};
/**
 * Takes each string in the array, puts into quotes and joins with a comma
 * [a,b,c] -> "a","b","c"
 *
 */
export declare function quotedJoin(toQuote: string[]): string;
