import { IProcessorSource, EventQuery } from './';
import { SubstrateEvent } from '@dzlzv/hydra-common';
export declare class GraphQLSource implements IProcessorSource {
    private graphClient;
    constructor();
    subscribe(events: string[]): Promise<void>;
    indexerHead(): Promise<number>;
    nextBatch(queries: EventQuery[], size: number): Promise<SubstrateEvent[]>;
}
export declare function collectQueries(queries: string[]): string;
export declare function getEventsGraphQLQuery({ events, extrinsics, block_gte, block_lte, id_gt }: EventQuery, limit: number): string;
