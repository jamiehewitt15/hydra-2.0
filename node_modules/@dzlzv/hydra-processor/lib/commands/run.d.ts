import { Command, flags } from '@oclif/command';
export default class Run extends Command {
    static flags: {
        manifest: flags.IOptionFlag<string | undefined>;
        indexer: flags.IOptionFlag<string | undefined>;
        env: flags.IOptionFlag<string | undefined>;
        id: flags.IOptionFlag<string | undefined>;
    };
    run(): Promise<void>;
}
