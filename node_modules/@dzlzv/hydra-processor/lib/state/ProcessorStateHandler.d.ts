import { EntityManager } from 'typeorm';
import { IProcessorStateHandler } from './IProcessorStateHandler';
import { IProcessorState } from './IProcessorState';
export declare class ProcessorStateHandler implements IProcessorStateHandler {
    persist(state: IProcessorState, em?: EntityManager): Promise<void>;
    init(blockInterval?: {
        from: number;
    }): Promise<IProcessorState>;
}
export declare function initState(blockInterval: {
    from: number;
} | undefined, lastState: {
    eventId: string;
    lastScannedBlock: number;
} | undefined): IProcessorState;
