"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const extract_1 = require("./extract");
const chai_1 = require("chai");
describe('events', () => {
    it('should parse arg types', () => {
        const argTypes = [
            'Vec<u32>',
            'AccountId & Codec',
            'Balance | LookupSource<Balance>',
        ];
        const types = extract_1.stripTypes(argTypes);
        console.log(types.join(','));
        chai_1.expect(types).to.include.members(['Vec', 'u32']);
        chai_1.expect(types).to.include.members(['AccountId', 'Codec']);
        chai_1.expect(types).to.include.members(['Balance', 'LookupSource']);
        chai_1.expect(types.length).to.equal(6, 'Should contain 6 types');
    });
});
