import { MetadataLatest } from '@polkadot/types/interfaces';
import { TypeRegistry } from '@polkadot/types/create';
import BN from 'bn.js';
import { TypeDefs } from './types';
export declare const registry: TypeRegistry;
export interface MetadataSource {
    source: string;
    blockHash?: string;
    spec?: ChainSpec;
}
interface ChainSpec {
    chain: string;
    name: string;
    version: BN;
}
export declare function getMetadata({ source, blockHash, }: MetadataSource): Promise<MetadataLatest>;
export declare function registerCustomTypes(typeDefs: string): TypeDefs;
export {};
