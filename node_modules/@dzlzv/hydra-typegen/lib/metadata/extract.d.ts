import { IConfig } from '../commands/typegen';
import { Vec } from '@polkadot/types/codec';
import { Text } from '@polkadot/types/primitive';
import { ModuleMeta } from './types';
export declare function extractMeta({ metadata: metadataSource, events, calls, }: IConfig): Promise<ModuleMeta[]>;
/**
 * Reduce all argument types to single types, e.g. Compact<Balance> to [Compact, Balance]
 *
 * @param e event
 */
export declare function stripTypes(argTypes: string[] | Text[] | Vec<Text>): string[];
