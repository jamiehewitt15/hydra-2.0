import { ModuleMeta } from '../metadata';
import { CustomTypes } from '../commands/typegen';
export declare type GeneratorConfig = {
    modules: ModuleMeta[];
    importsRegistry: ImportsRegistry;
    dest: string;
    customTypes?: CustomTypes;
    validateArgs: boolean;
};
export declare type TypeExist = Record<string, boolean>;
export declare type ImportsRegistry = Record<string, string>;
export declare type ImportsDef = Record<string, TypeExist>;
