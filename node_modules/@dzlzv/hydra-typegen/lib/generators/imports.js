"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildModuleImports = void 0;
function buildModuleImports({ types }, { importsRegistry }) {
    const importsDef = {};
    types.forEach((i) => {
        if (importsRegistry[i] === undefined) {
            throw new Error(`Cannot resolve import for type ${i}`);
        }
        if (!importsDef[importsRegistry[i]]) {
            importsDef[importsRegistry[i]] = {};
        }
        importsDef[importsRegistry[i]][i] = true;
    });
    return importsDef;
}
exports.buildModuleImports = buildModuleImports;
