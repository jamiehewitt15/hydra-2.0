export declare function createDir(path: string, del?: boolean, recursive?: boolean): void;
export declare function createFile(path: string, content?: string, replace?: boolean): void;
export declare function copyFiles(from: string, to: string): Promise<void>;
export declare function getTemplatePath(template: string): string;
/**
 * Copies the template to the current directory of the process under the <filename>
 *
 * @param template Template file int templates/<templateName>
 * @param fileName Filename of the file to be created
 */
export declare function copyTemplateToCWD(templateName: string, fileName: string): Promise<void>;
/**
 * resolve a package version by resolving package.json
 *
 * @param pkgName dependency to loockup
 */
export declare function resolvePackageVersion(pkgName: string): string;
/**
 * Tries to resolve a filepath from node_modules and reads the file as a string
 *
 * @param path a path like 'someModule/<path-to-file>
 */
export declare function readModuleFile(path: string): string;
/**
 * Parse stderr for warthog command `warthog codegen` (child process) and search for errors. If there is an error in the
 * generated/graphql-server/src/* then the command fails.
 * @param stderr string
 * @param regexExpr string
 */
export declare function parseWarthogCodegenStderr(stderr: string, regexExpr?: RegExp): void;
export declare function getWarthogDependency(): string;
